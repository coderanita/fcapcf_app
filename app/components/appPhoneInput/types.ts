import { DimensionValue } from "react-native";

export type T_APP_PHONE_INPUT = {
  control?: any;
  name: string;
  label?: string;
  placeholder?: string;
  width?: DimensionValue;
  errorText?: string;
};
